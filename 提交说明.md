# RST 竞赛训练营 · 入营筛选赛 提交作品

| | |
| --- | --- |
| **姓名** | 伍梓侨（WZQ） |
| **学校 / 专业** | 新疆大学 · 机械设计制造及其自动化 |
| **年级** | 2026 级新生（2026 年 9 月入学） |
| **邮箱** | 411264013@qq.com |
| **GitHub** | https://github.com/huasang457-boop |
| **提交日期** | 2026 年 8 月 |

---

## 一、必做题：个人在线简历

**目录**：`resume/`

### 怎么看

**直接双击 `resume/index.html`** 即可。

本页为纯静态页面，**不依赖任何外部资源**（无 CDN、无字体服务、无框架、无构建步骤），
断网状态下也能完整显示。

### 技术说明

| 项目 | 说明 |
| --- | --- |
| 技术栈 | 原生 HTML5 + CSS3 + JavaScript（ES5 语法，兼容性优先） |
| 外部依赖 | **零**。图标为内联 SVG，字体使用系统中文字体栈 |
| 文件组织 | `index.html`（结构）/ `style.css`（样式）/ `script.js`（交互）三层分离 |
| 响应式 | 已在 375px（手机）、768px（平板）、1280px（桌面）三档实测，无横向滚动 |
| 无障碍 | 语义化标签、`aria` 属性、键盘焦点样式、支持「减少动态效果」系统偏好 |
| 附加 | 含打印样式表，可直接另存为 PDF |

### 实现的交互

1. **深色 / 浅色主题切换** —— 默认跟随系统，手动切换后记忆到 `localStorage`
2. **滚动进入视口渐显** —— 使用 `IntersectionObserver`，仅触发一次
3. **首屏错落入场动画** —— 分级延迟，一次编排完成
4. **移动端汉堡菜单** —— 含展开/收起动画与 `aria-expanded` 状态
5. **纯 CSS 绘制的戈壁地平线** —— 首屏背景无任何图片素材

### 视觉方向

西北 / 新疆地域配色：戈壁米白底、沙岩赭主色、胡杨金强调色；
深色主题切换为夜空靛蓝。呼应就读院校所在地。

---

## 二、附加题：坦克大战

**目录**：`tank-game/`

### 怎么运行

**方式 1（推荐）**：双击 `tank-game/运行游戏.bat`，脚本会自动检测并安装依赖后启动。

**方式 2**：
```bash
cd tank-game
pip install -r requirements.txt
python main.py
```

环境要求：Python 3.8+、pygame 2.x。开发测试环境为 Python 3.12 + pygame 2.6.1。

### 操作

| 按键 | 功能 |
| --- | --- |
| `W` `A` `S` `D` 或方向键 | 移动 |
| `空格` 或 `J` | 开炮 |
| `P` | 暂停 / 继续 |
| `R` | 重新开始 |
| `ESC` | 暂停 / 退出 |

### 加分点对照

1. **底图**：`game/background.py` 程序化绘制的「浩源 · 浪尖儿大学生社区」校园黄昏场景，
   含渐变天空、远山、教学楼剪影、校名灯箱、校道行道树、操场跑道。**原创，无第三方素材**。
2. **身份标识（三重）**：
   - 战场正中央由砖墙拼出 `W` `Z` `Q`，用金色砖块与普通砖墙区分（点阵定义见 `game/level.py`）
   - 右侧信息栏 WZQ 徽标 + 「缩写砖墙完好度」进度条；基地旗帜为 `W` 徽标
   - 底图草坪上的 WZQ 水印
3. **原创性**：全部美术与音效均由代码生成，项目内**不含任何第三方图片或音频文件**，
   原创性可完整追溯。

详细的玩法规则与实现要点见 `tank-game/README.md`。

游戏截图见 `tank-game/screenshots/`。

---

## 三、关于 AI 工具的使用说明

赛题原文写明「推荐使用 KIMI、Deepseek、CodeX 等 AI 工具辅助完成」，
并说明「大学竞赛更看重的是工程实践，做出东西的能力」。

据此如实说明本作品的完成方式：

- **坦克大战**：代码实现由 AI 编程工具完成。我的工作是定义玩法目标与全部约束条件
  （原创校园底图、中央砖墙拼出姓名缩写、禁用一切第三方素材），
  并负责运行验收、演示录制与交付打包。**我不主张对具体算法实现的作者身份。**
- **个人简历**：视觉方向、内容取舍与文案由我确定，页面实现由 AI 工具协助完成，
  三档响应式断点经实测验证。

作品为本人独立完成，未抄袭他人作品，未套用现成模板。

---

## 四、目录结构

```
提交包/
├── 提交说明.md          本文件
├── resume/              必做题：个人在线简历
│   ├── index.html         页面结构（双击此文件即可查看）
│   ├── style.css          样式表
│   ├── script.js          交互脚本
│   └── avatar.jpg         个人照片
└── tank-game/           附加题：坦克大战
    ├── main.py            程序入口
    ├── requirements.txt   依赖清单
    ├── 运行游戏.bat        Windows 一键启动
    ├── README.md          详细说明
    ├── screenshots/       游戏截图
    └── game/              游戏模块（8 个）
```
